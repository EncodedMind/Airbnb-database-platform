# ΕΡΓΑΣΙΑ 2: ΣΧΕΔΙΑΣΗ ΚΑΙ ΧΡΗΣΗ ΒΑΣΕΩΝ ΔΕΔΟΜΕΝΩΝ

###### Δημήτριος Ανδρεάκης (1115202300008) - Σταματίνα Ναδάλη (1115202300137)

### 1. Check If Property Exists

> Σύνδεσμος συνομιλίας με ChatGPT: https://chatgpt.com/share/68129705-9944-8003-af4c-8187aae985f0

Η συνάρτηση `checkIfPropertyExists` ελέγχει αν υπάρχει ιδιοκτησία στην τοποθεσία `location_a` που είναι τύπου `property_type_a`. Το ερώτημα SQL επιστρέφει '`0`' ή '`1`' αναλόγως. Στη συνέχεια, αποθηκεύουμε αυτό το αποτέλεσμα σε μία μεταβλητή της python, ώστε να μπορέσουμε να ελέγξουμε το αποτέλεσμα για να επιστρέψουμε αναλόγως "`yes`" ή "`no`".

**Χρήση ChatGPT**: Το ChatGPT επιβεβαίωσε την ορθότητα του ερωτήματος σε SQL, συνέταξε τον κώδικα σε python, σύμφωνα με τα πρότυπα της βιβλιοθήκης `pymysql` και συνέβαλε στην εμφάνιση του αποτελέσματος, ακολουθώντας τις οδηγίες της εκφώνησης.

### 2. Select Top N Hosts

> Σύνδεσμος συνομιλίας με ChatGPT: https://chatgpt.com/share/68050b46-28d8-8006-ba1b-8503f35fed66

Η συνάρτηση `selectTopNHosts` εντοπίζει για κάθε τύπο καταλύματος τους `N` ιδιοκτήτες με τις περισσότερες καταχωρίσεις αυτού του τύπου και επιστρέφει λίστα από tuples της μορφής (`type_name`, `host_id`, `property_count`). Αν για κάποιον τύπο υπάρχουν λιγότεροι από `N` ιδιοκτήτες, επιστρέφονται όλοι.

Το SQL ερώτημα επιστρέφει όλες τις εγγραφές `type_name`, `host_id`, `property_count` ταξινομημένες σε φθίνουσα σειρά ως προς τον αριθμό καταχωρίσεων. Στη συνέχεια, με Python, επιλέγουμε τους `N` κορυφαίους ιδιοκτήτες για κάθε τύπο.

**Χρήση ChatGPT**: Χρησιμοποιήσαμε το ChatGPT για επαλήθευση του ερωτήματος, επιλογή των top `N` και βελτίωση του README. Ειδικότερα, τροποποιήσαμε την απάντηση του ChatGPT, ώστε τα αποτελέσματα να επιστρέφονται ως tuples αντί να τυπώνονται στην οθόνη.

### 3. Find Matching Properties

> Σύνδεσμος συνομιλίας με ChatGPT: https://chatgpt.com/share/6809f3e4-2cf8-8006-81ec-e92ad79ebf42

Η συνάρτηση `findMatchingProperties` δέχεται ως όρισμα ένα `guest_id` και επιστρέφει τα IDs, τις παροχές και τους κανόνες όλων των καταλυμάτων που πληρούν τις ακόλουθες προϋποθέσεις:
1. Έχουν τουλάχιστον μία παρόχη που υπάρχει σε κάποιο κατάλυμα στη λίστα επιθυμιών του χρήστη `guest_id`.
2. Έχουν τουλάχιστον έναν κανόνα που υπάρχει σε κάποιο κατάλυμα στο οποίο ο χρήστης `guest_id` έχει κάνει κράτηση.
3. Ο οικοδεσπότης δεν είναι ο ίδιος με κανέναν από τους οικοδεσπότες των καταλυμάτων στα οποία ο `guest_id` έχει κάνει κράτηση. 

**Χρήση ChatGPT**: Χρησιμοποιήσαμε το ChatGPT για την επαλήθευση των ερωτημάτων και τη σωστή διαχείριση των exceptions στη Python.

### 4. Count Words For Properties

> Σύνδεσμος συνομιλίας με ChatGPT: https://chatgpt.com/share/6808dae7-fb40-8006-b32b-d18ce04c7367

Η συνάρτηση `countWordsForProperties` επιστρέφει πληροφορίες (id, όνομα, τοποθεσία, αριθμός μοναδικών επισκεπτών και διαθέσιμες παροχές) για τα ακίνητα που πληρούν τις ακόλουθες προϋποθέσεις:
1. Τουλάχιστον `Ν` διαφορετικοί χρήστες έχουν κάνει κράτηση.
2. Τουλάχιστον ένας από τους χρήστες που έχουν κάνει κράτηση, έχει αφήσει κριτική για το κατάλυμα.
3. Το κατάλυμα έχει τουλάχιστον 2 διαφορετικές παροχές.
4. Το κατάλυμα δεν βρίσκεται στη λίστα επιθυμιών κανενός χρήστη.

Μετά το φιλτράρισμα των καταλυμάτων, η συνάρτηση συλλέγει, για καθένα από αυτά, τα κείμενα όλων των κριτικών που έχουν γίνει από χρήστες με κράτηση στο συγκεκριμένο κατάλυμα. Έπειτα, υπολογίζει τη συχνότητα κάθε λέξης στα κείμενα αυτών των κριτικών και επιστρέφει τις `M` πιο συχνά χρησιμοποιούμενες λέξεις, εξαιρώντας κοινές αντωνυμίες, συνδέσμους και μόρια (stop-words). 

**Χρήση ChatGPT**: Το ChatGPT βοήθησε στη δημιουργία του SQL ερωτήματος με τη σταδιακή προσθήκη των διαφόρων περιορισμών. Επίσης, μας συμβούλεψε για το αν ο διαχωρισμός του φιλτραρίσματος των καταλυμάτων από την ανάκτηση των κριτικών είναι καλή πρακτική. Τέλος, σε ένα σημείο χρειάστηκε να τροποποιήσουμε την απάντησή του, καθώς το ερώτημα που δημιούργησε δεν έδινε σωστά αποτελέσματα για τον αριθμό των χρηστών που έχουν κάνει κράτηση στο κατάλυμα. 

### 5. Find Common Properties And Guests

> Σύνδεσμος συνομιλίας με ChatGPT: https://chatgpt.com/share/68129769-0b64-8003-91f4-8a6b3eacf5cf

Η συνάρτηση `findCommonPropertiesAndGuests` βρίσκει τα κοινά καταλύματα που έχουν μείνει δύο διαφορετικοί επισκέπτες, με την προϋπόθεση ότι ο ένας από αυτούς έχει μείνει σε ένα ακίνητο που είχε μείνει και ο `A` και ο άλλος έχει μείνει σε ένα ακίνητο που είχε μείνει και ο `B`. Το ερώτημα υλοποιήθηκε σε SQL με τέτοιον τρόπο, ώστε να μην αποκλείεται ο ένας χρήστης να είναι ο ίδιος ο `A` ή/και ο άλλος να είναι ο `B`. 

**Χρήση ChatGPT**: Το ChatGPT δεν αποδείχθηκε τόσο βοηθητικό για τη συγκεκριμένη συνάρτηση, καθώς απέρριπτε το αρχικό ερώτημα, το οποίο αποδείχθηκε στην πράξη πως είναι σωστό. Ύστερα από μερικές ανταλλαγές επιχειρημάτων, το ChatGPT δέχθηκε και τη δική μας εκδοχή, οπότε και την ενέταξε στο πρόγραμμά μας σε python, με τα πρότυπα της βιβλιοθήκης `pymysql`.

### 6. High Value Host

> Σύνδεσμος συνομιλίας με ChatGPT: https://chatgpt.com/share/68129779-4828-8003-82b2-1d8016860476

Η συνάρτηση `highValueHost` επιστρέφει τα καταλύματα "υψηλής αξίας", όπως αυτά ορίζονται από την εκφώνηση. Η εύρεσή τους γίνεται μέσω ενός περίπλοκου ερωτήματος σε SQL. Για να το απλοποιήσουμε, το σπάσαμε σε επιμέρους ερωτήματα, τα οποία στη συνέχεια συνδυάσαμε:
α) Εύρεση των καταλυμάτων των οικοδεσποτών που είναι υψηλής αξίας.
β) Εύρεση των καταλυμάτων που έχουν κλείσει επισκέπτες που είναι υψηλής αξίας.
γ) Εύρεση των κοινών καταλυμάτων (από τα α και β).
δ) Εύρεση των παροχών των καταλυμάτων του γ.
ε) Εύρεση της συχνότητας της κάθε παροχής του δ.
στ) Ταξινόμηση των παροχών σε φθίνουσα σειρά συχνότητας, όπως αυτή ορίζεται στο ερώτημα ε.
ζ) Εμφάνιση παροχών και αντίστοιχων συχνοτήτων (πρόκειται για το αποτέλεσμα της εξωτερικής `SELECT` του ερωτήματος στ).

**Χρήση ChatGPT**: Το ChatGPT μάς βοήθησε στη συγγραφή των υπο-ερωτημάτων. Συγκεκριμένα, εντόπισε μερικά περιττά σημεία. Επιλέξαμε να μην τα αφαιρέσουμε, καθώς η δική μας αρχική εκδοχή είναι πιο "πιστή" στην εκφώνηση. Επιπλέον, μάς αποκάλυψε τον τρόπο να ταξινομούμε τα αποτελέσματα μίας εντολής `SELECT`. Τέλος, αφού επιβεβαίωσε την ορθότητα του τελικού ερωτήματος, συνδυάζοντας τα επιμέρους υπο-ερωτήματα, συνέταξε το ερώτημα σε κώδικα python, καθιστώντας τη συνάρτηση λειτουργική.

### 7. Recommended Property

> Σύνδεσμος συνομιλίας με ChatGPT: https://chatgpt.com/share/6809dfe4-bd38-8006-85d5-c61e802abc1b

Η συνάρτηση `recommendedProperty` βρίσκει όλα τα ακίνητα τα οποία:
1. Βρίσκονται στην πόλη `desired_city`.
2. Κοστίζουν το πολύ `min_price`.
3. Έχουν τουλάχιστον `min_rating` βαθμολογία.

Έπειτα, υπολογίζεται το `amenity_score`, αφού πρώτα κανονικοποιηθούν τα βάρη που έχει δώσει ο χρήστης στις παροχές με βάση το min-max scaling. Τέλος, χρησιμοποιείται η εκθετική συνάρτηση για να συνυπολογιστούν το `amenity_score`, η βαθμολογία του καταλύματος και η τιμή του σε ένα ενιαίο score. Το κατάλυμα με το υψηλότερο score είναι το πιο κατάλληλο για τον χρήστη και αποθηκεύεται ως `best_property`.

Στη συνέχεια, ελέγχουμε αν το `best_property` υπάρχει ήδη σε κάποια λίστα επιθυμιών του χρήστη. Με αυτόν τον τρόπο αποφεύγουμε τις διπλότυπες εγγραφές. Αν δεν υπάρχει, δημιουργούμε μία νέα public wishlist για τον συγκεκριμένο χρήστη. Τέλος, συνδέουμε τη wishlist που δημιουργήσαμε με το `best_property`, εισάγοντας την σχετική εγγραφή στον πίνακα `wishlist_has_property`.

**Χρήση ChatGPT**: Το ChatGPT μας βοήθησε κυρίως στο κομμάτι της προσθήκης δεδομένων, μας πρότεινε τρόπους να ελέγξουμε ότι η νέα λίστα επιθυμιών πράγματι προστέθηκε και μας εξήγησε γιατί είναι απαραίτητο να ελέγχουμε για διπλότυπες εγγραφές, εφόσον αυτό δεν αποφεύγεται από την ίδια τη βάση.
